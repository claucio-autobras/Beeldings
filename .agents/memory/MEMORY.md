- [BlueBee setup gotchas](bluebee-setup.md) — npm firewall lockfile, Agent .replit, Supabase placeholder env, git-delete guard, checkpoint-rollback deleted modules (restore via git show/cat-file), mock contracts per restored page.
- [Infraspeak API](infraspeak-api.md) — Stoplight docs are a JS SPA (read via screenshot tool, not fetch); base URL/resource/fields not public; integration is config-driven.
- [Infraspeak ativa](bluebee-infraspeak-active.md) — PAT sandbox só funciona em api.sandbox.infraspeak.com/v3 (401 no host errado ≠ token inválido); failures em JSON:API; env stale sobrevive a restart de workflow.
- [BACnet write race](bluebee-bacnet-write-race.md) — register pending in the command_id Map BEFORE publishing; QoS2 publish-ack to remote broker is slower than the gateway's result, so late-registration drops the response → deterministic 20s timeout.
- [Gateway agent download](bluebee-gateway-agent-package.md) — GET /gateways/agent-package zips gateway source on demand; backend is CJS (use adm-zip, not ESM archiver); ships source+npm, not .exe; needs apps/gateway at runtime.
- [Sessão cookie HttpOnly](bluebee-session-cookie.md) — JWT só no cookie `bluebee_session`; fetches precisam credentials:'include'; header Authorization segue como fallback; proxy Replit reescreve SameSite.
- [BlueBee auth local](bluebee-auth.md) — saímos do Supabase; JWT sempre foi do backend; dados no Postgres; senhas antigas viviam no Supabase → usuários legados ficam sem senha (precisam reset antes da prod).
- [BlueBee audit trail](bluebee-audit-trail.md) — global interceptor uses an explicit route allowlist; login audited in AuthService (needs FAILURE); never store req.body (password); IP masked only on display.
- [BlueBee virtual points](bluebee-virtual-points.md) — Bancada = Device protocol='virtual'; NEVER count/list as real equipment; merge EXCLUDE_VIRTUAL_DEVICES in every real-device query; SimulatorService is the only virtual consumer.
- [Task merge DB sync](bluebee-task-merge-db-sync.md) — merges add schema/migration files but post-merge runs only npm install; must `prisma migrate deploy` + `generate` + restart on main, else new tables missing & client stale (fire-and-forget writes fail silently).
- [Claucio GitHub sync](bluebee-claucio-snapshot.md) — bidirectional via sync point in .git/bluebee-github-sync; push replays commits (never full history/force after base), pull applies remote diff as local commit.
- [BACnet COV](bluebee-bacnet-cov.md) — node-bacnet subscribeCov tem 8 args (options+cb); notificação via evento covNotifyUnconfirmed; COV points excluídos do polling de propósito; telemetria idêntica; correlacionar por subscribeId+address+objeto.
- [Modbus RTU serial](bluebee-modbus-rtu.md) — RS485 half-duplex = 1 fila serializada por porta no gateway; setID antes de cada op; connectionType ausente = TCP; arquivo novo no gateway exige bump + manifest --update.
- [Escrita Modbus](bluebee-modbus-write.md) — só holding/coil; sem relinquish/prioridade (conceito BACnet); gateway reverte scale/offset e publica readback como telemetria; nunca confirmar por readback.
- [Modbus block reads](bluebee-modbus-block-reads.md) — gateway agrupa registradores contíguos por tipo em 1 range read; payload/ordem devem ficar idênticos (reordena por originalIndex, NUNCA por tag); split-on-error.
- [Deploy de produção](bluebee-production-deploy.md) — VM sempre-ligado; .replit só via verifyAndReplaceDotReplit (temp dentro do workspace); setEnvVars grava texto puro no .replit → senha só como Secret; seed compilado, não reseta senha do admin.
- [AlarmEvent nullable alarmRule](bluebee-nullable-alarmrule.md) — table holds ALARM + AUTOMATION_NOTICE; telemetry-only consumers MUST scope `kind:'ALARM'` then assert `e.alarmRule!`; bell shim returns both kinds.
- [Dashboard alarm cards](bluebee-dashboard-alarm-cards.md) — 3 alarm-row cards share fixed `ALARM_CARD_H`, never stretch; overflow via item-limit/modal/internal-scroll; dashboard→alarms deep-links reuse `severity`/`state`/`highlight` query params.
- [Telemetry socket compartilhado](bluebee-telemetry-socket.md) — /telemetry no frontend é singleton ref-counted (acquire/release + linger); hooks nunca chamam io()/disconnect(); tenant muda via setTelemetryScope sem reconectar.
- [Socket tenant scoping](bluebee-socket-tenant-scope.md) — per-tenant rooms; telemetry/ALARM emit direct on every instance, only leader-only AUTOMATION_NOTICE goes via cluster bus (else double-emit); Socket.IO auth in afterInit middleware, not handleConnection.
- [BACnet RPM polling](bluebee-bacnet-rpm.md) — poll via ReadPropertyMultiple in batches; split-in-half on failure to tell APDU-oversize from no-RPM; only mark rpmUnsupported when single-object RPM fails but individual read works; keep telemetry payload identical; RPM needs timeout guard.
- [Somas de Alarme](bluebee-alarm-groups.md) — alarm groups are site-scoped; aggregate (active/total/severity) derived live at read, never persisted; projectId filter resolves project→site, response projectId always null.
- [SVG scaling text labels](bluebee-svg-scaling-text.md) — AlarmsOverTimeChart viewBox scales SVG <text> tiny on narrow cards; render axis/value labels as fixed-size HTML overlays positioned via % of CHART_W/CHART_H.
- [SCADA equipment renders](bluebee-scada-equipment-renders.md) — equipment = isometric PNG renders in public/scada-equipment; state via glow+LED, never recolor; tank level = overlay gauge; forbid brand text in prompts.
- [SCADA global components](bluebee-scada-components.md) — "Meus Componentes" = tabela por tenant via /scada/components; settings.components é legado só-leitura; migração one-off idempotente pendente em prod.
- [SCADA pinned bars dock](bluebee-scada-pinned-dock.md) — barras fixas ficam FORA da folha em editor/preview/viewer com clamps compartilhados; folha x=0 = borda da sidebar (WYSIWYG).
- [SCADA editor static render](bluebee-scada-editor-static-render.md) — widgets read null as "offline grey"; edit-mode design look needs an explicit staticRender flag threaded to each *WidgetView, not just a null getter.
- [BACnet sync replace](bluebee-bacnet-sync-replace.md) — replace payload É o conjunto final (nunca existentes+novos); merge nome/tag por objectType:instance; delete+create recria IDs → trends/alarmes dos sobreviventes se perdem.
- [Alarm occurrence reuse](bluebee-alarm-occurrence-reuse.md) — reativação antes do ACK reusa a ocorrência NORMALIZED_UNACK (nunca cria linha nova); ops de banco do motor serializadas por regra via promise chain.
- [Gateway liveness LWT+heartbeat](bluebee-gateway-liveness.md) — explicit online/offline via retained status topic (LWT+15s heartbeat) overrides telemetry recency in DeviceStatusService; retain flag = survives backend restart; offline wins unless telemetry arrived after it.
- [Gateway health summary](bluebee-gateway-health.md) — separate additive channel `.../health` (ephemeral publish, no store-and-forward); never affects liveness; backend keeps last summary per gateway in-memory; UI shows stale/missing (>90s) as neutral "sem dados".
- [SCADA unified binding](bluebee-scada-unified-binding.md) — ONE point per widget via readWidgetBinding/writeWidgetBinding (legacy status/visibility fallback); status/anim/visibility reuse it; command widgets keep own selectors; `fade` animation added.
- [User preferences & dark mode](bluebee-user-preferences.md) — prefs JSONB por usuário; Tailwind v4: cores em `@theme` sem `inline` p/ override sob `.dark`; PATCH inválido preserva atual; Turbopack pode exigir rm -rf .next.
- [Sensitive-delete confirmation](bluebee-sensitive-deletes.md) — 7 critical DELETEs need `X-Sensitive-Action-Token` (5-min JWT from confirm-password); new critical deletes must add guard + PasswordConfirmDialog in lockstep.
- [API hardening](bluebee-api-hardening.md) — throttler tracker must use XFF (loopback behind Next proxy = one shared bucket) and skip ws contexts; CORS centralizado em resolveCorsOrigins; Helmet CORP cross-origin p/ imagens SCADA.
- [BACnet routed addressing](bluebee-bacnet-routing.md) — net/adr de MS/TP atrás de roteador vive em Device.config (net>0 = válido); discovery tiers objectList→index→scan devem propagar discoverySource até a UI.
- [MQTT sample capture](bluebee-mqtt-sample-capture.md) — frontend suggestion heuristic must mirror gateway jsonPath resolution; warnings are non-blocking; test capture with a temp publisher workflow (bash bg dies).
- [BACnet force value/relinquish](bluebee-bacnet-force-value.md) — só comandáveis (AO/AV/BO/BV/MSO); Liberar=write null ponta-a-ponta (tag Null 0); NUNCA confirmar relinquish por readback (Number(null)=0 = falso positivo); prio default 8.
- [SCADA assets no App Storage](bluebee-scada-app-storage.md) — bucket GCS via sidecar; URL /scada-assets/<tenant>/<arquivo> imutável (vive no JSON das telas); PRIVATE_OBJECT_DIR obrigatório; prod roda migrate script 1x.
- [CFTV via ONVIF](bluebee-cftv-onvif.md) — Device protocol='onvif'; senha AES-GCM só decifrada no config MQTT; probe-first no cadastro auto-preenche fabricante/modelo; eventos motion/tamper/video_loss = pontos digitais.
- [CFTV saúde híbrida SNMP](bluebee-cftv-hybrid-snmp.md) — câmera ONVIF pode ter canal SNMP opcional (pontos objectType='snmp', perfil de OIDs por fabricante); falha SNMP publica null e NUNCA afeta o STATUS ONVIF.
- [SNMP v1 batch GET](bluebee-snmp-v1-batch.md) — um OID inválido no lote derruba o GET inteiro (tudo null com STATUS=1); polling exclui `unsupported` e faz fallback por OID (split-on-error).
- [SNMP value parsing](bluebee-snmp-value-parsing.md) — Hikvision responde "45 PERCENT"/"256 MB" (OCTET STRING); parseSnmpNumber central no gateway; .1.9.0 é storage, RAM real em .1.11.0.
- [CFTV via SNMP](bluebee-cftv-snmp.md) — câmeras são Devices protocol='snmp' excluídos das queries BMS; saúde = valor do STATUS (não recência); alarmes protocol-agnósticos; widget 'camera' com case explícito.
- [SNMP diagnose progress](bluebee-snmp-diagnose-progress.md) — progress Maps polled por HTTP em cluster: gateway publica 0/N imediato; resultado marca done em TODA instância antes do check de pendência.
- [SNMP erro = resposta](bluebee-snmp-error-is-response.md) — noSuchObject/noSuchName prova câmera viva (só timeout é silêncio); getNext fallback no ping; cause community vs no_response; binding.unsupported.
- [SNMP camera scan](bluebee-snmp-scan.md) — fallback combos rodam em paralelo por host (senão estoura timeout); TCP-alive via RST (loopback sempre "vivo"); progresso via scanId do cliente + polling.
- [Gateway local + câmera SNMP simulada](bluebee-local-gateway-sim.md) — testar features de campo rodando o gateway real local + agente net-snmp; config de device é tópico retido (restaurar banco e reiniciar backend no fim).
- [CFTV last-value seed](bluebee-cftv-last-value.md) — lastValue persisted só p/ câmeras; live vence seed; ONVIF applyConfig diffa config e só reinicia câmera nova/alterada; novos pontos default exigem create+backfill.
- [Gateway OTA update](bluebee-gateway-ota.md) — serviço roda run.js (launcher, nunca atualizado via OTA); aceite SÓ por confirmação MQTT (pending-verify), watchdog 10min reverte; rota pública de download antes do :id senão 401.
- [Camera multi-vendor telemetry](bluebee-camera-multivendor.md) — brand logic só no provider-registry; fallback POR campo; real > estimativa; Intelbras = árvore Dahua bestEffort com sentinela 0 → unreliable; sim local para células de tabela usa provider Table, não Scalar.
- [jsPDF Unicode fonts](bluebee-pdf-fonts.md) — Roboto TTF embutida via pdf-fonts.ts + nest-cli assets; "�" restante é U+FFFD gravado no banco (ingestão BACnet), não é fonte.
- [Report timezone](bluebee-report-timezone.md) — todo output de relatório formata com timeZone America/Sao_Paulo (helper central); datetime-local parseado como SP no front, fim do período inclusivo até :59.999.
- [Alarm period = last activity](bluebee-alarm-period-activity.md) — report & alarm list filter/sort by max(activated, normalized, lastReactivated) via shared util; activatedAt-only hides reactivated CFTV alarms.
- [Vídeo comercial video-js](bluebee-video-comercial.md) — TTS 500 exige retry; única voz pt-BR ScheilaSMTy; medir VO com ffprobe e retunar cenas ANTES de mixar composite; durações e composite mudam em lockstep.
- [Tenant inactive semantics](bluebee-tenant-inactive.md) — 403 TENANT_INACTIVE em login+JWT; feeds globais de alarme excluem tenants inativos só SEM filtro explícito; socket drop via canal tenant_status em toda instância; admins globais imunes.
- [Histórico de comandos](bluebee-command-history.md) — GET /commands deriva de audit_logs action='COMMAND'; write responde 200 com success:false → interceptor precisa gravar FAILURE; pointLabel só p/ trilha.
- [Histórico de automações](bluebee-automation-runs.md) — AutomationRun: FK cascade (delete da regra apaga histórico), recordRun fire-and-forget, CONTINUOUS só grava quando age, retenção 90d na escrita.
- [Frontend i18n](bluebee-i18n.md) — pt string é a chave do dicionário; useT() lê o store de preferências (troca sem reload); dicionário por módulo; cuidado com shadowing de `t` em .map.
- [EMQX broker monitor](bluebee-emqx-monitor.md) — alertas usam só descarte relevante (no_subscribers é benigno e domina o total); avisos de sistema via tenant sentinela `__system__`; histerese/cooldown.
- [Trend rollups & partições](bluebee-trend-rollups.md) — SUM não AVG (merge exato); cursor global: nunca rodar rollup concorrente; retenção dropa partições; trends padrão nunca alteram existentes; timeline >3d usa rollup horário.
- [Relatório de Disponibilidade](bluebee-availability-report.md) — status_events só transições deduplicadas; sweeper líder com carência 90s pós-boot; câmera = STATUS+recência; sem cobertura = "Sem dados", nunca 0/100% fake.
- [MQTT boolean coerce](bluebee-mqtt-boolean-coerce.md) — valueType default 'number' + payload JSON booleano = null silencioso (Number("true")=NaN); coerce numérico deve mapear bool/"true"/"false" → 1/0.
- [Escrita MQTT (Shelly RPC)](bluebee-mqtt-write.md) — comando/resposta sob …/sensors/ com validação dupla; template {{value}}/{{id}}; PUT de ACL do EMQX exige username no body.
- [Dispensa de notificações](bluebee-notification-dismissals.md) — dispensas do sino em users.preferences JSONB; chave inclui lastReactivatedAt/lastCommunication (reativação reusa id); TTL 30d/500; dispensar ≠ ACK.
- [SCADA click action](bluebee-scada-click-action.md) — executa só com isEditor=false E sem onClick; excluded-types p/ widgets interativos; toast via editor.store global + ToastNotification montado.
- [ACK note required](bluebee-ack-note-required.md) — motivo obrigatório no ACK (individual e lote); sino legado usa requireNote:false; bulk ACK pula itens não reconhecíveis e o diálogo só limpa seleção no sucesso.
- [SCADA optimistic commands](bluebee-scada-optimistic-commands.md) — pending store compartilhado por deviceId|tag via getValue (nunca ler telemetria direto); gateway ≥v1.4.0 publica readback pós-escrita como telemetria.
- [SCADA cor transparente](bluebee-scada-transparent.md) — 'transparent' é valor válido em campos de cor; nunca concatenar alpha em hex direto, usar isTransparentColor/scadaColorWithAlpha e desativar glow/borda.
- [Tenant branding topbar](bluebee-tenant-branding.md) — logo reusa pipeline de assets SCADA (App Storage + /scada-assets); cor é só acento, nunca fundo de texto; sem logo = só nome (nunca iniciais).
- [lastCommunication durável](bluebee-last-communication.md) — nunca createdAt/updatedAt; memória → fallback durável (status_events + lastValueAt) via resolveLastSeen[Many]; sem dado = null → "sem dados".
- [SCADA dark chrome](bluebee-scada-dark-chrome.md) — dark-by-design SCADA surfaces precisam da classe `scada-dark-chrome` (restaura slates sob `.dark` + color-scheme dark p/ selects nativos).
- [KB PDF ingestion](bluebee-kb-pdf-ingest.md) — texto de PDF traz NUL (Postgres rejeita) e multer decodifica filename como latin1; extract-pdf é só preview, criação vai no POST /knowledge.
- [Fase 3 — auditoria dados/UX/obs](bluebee-fase3-audit.md) — sem retenção em audit_logs/alarm_events/telemetry (status_events TEM, 365d); prod migra só via Publish sync (schema aditivo!); ACK de ativo ausente na UI; modais sem Escape/trap.
- [Fase 2 — auditoria de carga](bluebee-fase2-load-audit.md) — 2 instâncias duplicam trends/alarmes (ingestão sem guarda de líder); harness em .local/audit/tmp; bg procs morrem entre ShellExecs.
- [SCADA dashboard widgets](bluebee-scada-dash-widgets.md) — cards com cores explícitas (canvas ≠ tema), histórico via trend do ponto (sem trend = "sem dados"), multi-ponto fora do binding unificado, event-feed usa screenScope.
- [Tenant scope resolution](bluebee-tenant-scope.md) — services treat tenantId ''/undefined como "sem filtro"; todo controller usa resolveTenantScope (403 p/ cliente sem tenant), nunca `user.tenantId ?? ''`.
- [SCADA pipe widget](bluebee-scada-pipe.md) — vértices normalizados ao bbox; fluxo via WAAPI (CSS var em @keyframes congela em alguns browsers); sem binding = decorativo; edição por vértices, não alças.
- [Critical assets card](bluebee-critical-assets.md) — is_critical em device/ponto; runtime só de ponto opRole status c/ trend; câmera STATUS gated por device vivo; deep-link por perfil; payload = fonte da IA.
- [Primeira ação IA](bluebee-first-action.md) — rulesOverride de prompt deve valer TAMBÉM no ramo sem hits de RAG (senão perde JSON estrito + proibição destrutiva); falha da IA degrada p/ contexto factual, nunca 5xx.
- [MQTT tópico raiz próprio](bluebee-mqtt-root-topic.md) — modo root em Device.config; credencial dev-{id} + ACL restrita; gateway ACL rebuild p/ assinar roots; presença via device-heartbeat vence recência de telemetria.
- [Headless fullscreen testing](headless-fullscreen-testing.md) — headless Chromium não esconde portais no body em fullscreen; validar com fs.contains(overlay), nunca screenshot; hook usePortalContainer p/ modais SCADA.
